export interface LogoDevCompanyItem {
    name?: string;
    domain?: string;
    logo_url?: string;
  }
  
  export interface CompanySearchResult {
    name: string;
    domain?: string;
    logo_url?: string;
  }